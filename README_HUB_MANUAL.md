# 🌐 Micro IoT Dashboard Hub (Central Command)

**Version:** 1.0 (Genesis Era)
**Framework:** Next.js 16 (App Router) + Tailwind CSS 4
**Port:** 3000

---

## 📖 1. บทนำ (Introduction)
**Micro IoT Dashboard Hub** คือ "ศูนย์บัญชาการกลาง" (Central Command) ของระบบ GhostMicro ที่ทำหน้าที่รวบรวมทางเข้าสู่ Dashboard ย่อยทั้ง 8 ตัว และเครื่องมือบริหารจัดการ AI (Neuro Lab) ไว้ในที่เดียว
ออกแบบมาในธีม **Cyberpunk Glassmorphism** ที่เน้นความล้ำสมัย สวยงาม และดุดัน เพื่อให้ผู้ใช้รู้สึกเหมือนเป็นผู้ควบคุมระบบปฏิบัติการแห่งโลกอนาคต

---

## ⚙️ 2. หลักการทำงาน (System Architecture)
Hub ทำหน้าที่เป็น **Front-end Portal** ที่เชื่อมต่อกับบริการต่างๆ ดังนี้:

### A. การเชื่อมต่อภายนอก (External Connections)
Hub ไม่ได้ทำงานโดดเดี่ยว แต่ต้องอาศัย "เพื่อนร่วมงาน" เพื่อให้ทำงานได้สมบูรณ์:
1.  **Genesis Core (The Brain)** 🧠
    *   **ที่อยู่**: `http://localhost:8000` (หรือ IP ของเครื่อง Server)
    *   **หน้าที่**: ส่งข้อมูลสถานะระบบ (Status), รายชื่อ AI Personas, และความทรงจำ (Memories) มาแสดงบนหน้า Hub
    *   **สำคัญ**: หาก Genesis Core ไม่ทำงาน หน้า **Neuro Lab** จะหมุนติ้วและใช้งานไม่ได้
2.  **Dashboard Satellites (1-8)** 🛰️
    *   **ที่อยู่**: `https://micro-iot-dashboard-[1-8].vercel.app/`
    *   **หน้าที่**: เป็นหน้าจอแยกสำหรับการควบคุมหุ่นยนต์แต่ละตัว Hub ทำหน้าที่แค่ "ชี้ทาง" (Link) ไปหาหน้าเหล่านี้เท่านั้น

### B. โครงสร้างภายใน (Internal Structure)
*   **Grid Layout**: หน้าแรกใช้ Grid 4 Column เพื่อแสดง Card ของ Dashboard ทั้ง 8 ตัว
*   **Neuro Lab (`/neuro-lab`)**: หน้าลับสำหรับจัดการ AI (เปลี่ยนชื่อ, แก้ Instruction) โดยยิง API ผ่าน Socket.IO ไปหา Genesis Core

---

## 🚀 3. วิธีการใช้งาน (User Manual)

### 3.1 การเริ่มต้นระบบ (Startup)
⚠️ **ข้อจำกัดสำคัญ**: Hub จะต้องเปิดคู่กับ `micro-iot-ai-robot` (Genesis Core) เสมอ เพื่อให้ดึงข้อมูลได้ครบถ้วน

1.  **เปิด Genesis Core (Backend)**:
    ```bash
    # ต้องรันไว้ก่อนเสมอ (ปกติจะเป็น Service อัตโนมัติ)
    sudo systemctl start genesis-core
    ```
2.  **เปิด Dashboard Hub (Frontend)**:
    ```bash
    cd micro-iot-dashboard-hub
    npm run dev
    # เข้าใช้งานที่ http://localhost:3000
    ```

### 3.2 เมนูหลัก (Main Interface)
*   **Status Indicators**: มุมขวาบนแสดงสถานะ `SYSTEM: ONLINE` (ถ้าเขียวแปลว่าเชื่อม Genesis ติด)
*   **Dashboard Cards**: คลิกที่การ์ดเพื่อกระโดดไปยัง Dashboard ของหุ่นยนต์แต่ละตัว (Link จะเปิด Tab ใหม่)
*   **Neuro Lab Button**: ปุ่ม "NEURO LAB" ด้านบน ใช้สำหรับเข้าห้องผ่าตัดสมอง AI

### 3.3 แผงควบคุม Neuro Lab
ใช้ออกแบบ "ตัวตน" ของ AI:
*   **Sync Status**: แสดงสถานะการเชื่อมต่อกับสมอง
*   **Editor Form**:
    *   **Select AI**: เลือก ID หุ่นยนต์ (0-8)
    *   **Name**: ตั้งชื่อเล่นใหม่ให้หุ่น
    *   **Personality**: ใส่ Prompt หรือคำสั่งจิตวิญญาณ
*   **Memory Core**: ดูว่า AI จำอะไรได้บ้าง (และกดลบได้ถ้ามันจำผิด)

---

## 🛠️ 4. การจัดการไฟล์และการปรับแต่ง
ไฟล์สำคัญในโปรเจกต์:
*   `app/page.tsx`: หน้าแรก (Grid Menu)
*   `app/neuro-lab/page.tsx`: หน้าแก้ไข AI
*   `lib/socket.ts`: ตัวจัดการ Socket Client ที่คุยกับ Genesis (พอร์ต 8000)
*   `data/dashboards.json`: เก็บข้อมูล Config ของ Link Dashboard ทั้ง 8 ตัว (เช่น สีประจำตัว, ชื่อธีม)

---

## ⚠️ 5. Troubleshooting (ปัญหาที่พบบ่อย)

| อาการ                            | สาเหตุ                                     | วิธีแก้                                                                                                                                                    |
| :------------------------------- | :---------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Neuro Lab หมุนติ้ว (Loading...)** | Genesis Core (Port 8000) ยังไม่เปิด          | รัน `sudo systemctl start genesis-core`                                                                                                                  |
| **กด Link แล้วไม่ไป หรือ 404**      | Dashboard ลูก (Vercel) อาจมีปัญหา หรือ URL ผิด | ตรวจสอบสถานะ Deployment บน Vercel หรือแจ้งผู้ดูแล                                                                                                            |
| **หน้าเว็บขาวโพลน**                | `npm run dev` ชนกับ Port อื่น                | สั่ง `fuser -k 3000/tcp` เพื่อเคลียร์ Port แล้วรันใหม่ หรือเข้าใช้งานผ่าน [https://micro-iot-dashboard-hub.vercel.app/](https://micro-iot-dashboard-hub.vercel.app/) |

> **Tech Note**: Hub ใช้ Socket.IO-Client เวอร์ชันล่าสุด ต้องตรวจสอบให้แน่ใจว่าฝั่ง Python Server (Genesis) ใช้ `python-socketio` ที่รองรับ EIO=4 (Engine.IO v4)
